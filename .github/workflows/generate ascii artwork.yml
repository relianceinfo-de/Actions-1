name: Generate ASCII

on: push

jobs:
  ascii_job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4.1.7

    - name: List Repo Files
      run: ls -ltra
        
    - name: Executing shell script
      run: ./ascii-script.sh
